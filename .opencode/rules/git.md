# Git Conventions
- Branch naming: `feature/`, `fix/`, `chore/`, `refactor/`
- Commit style: conventional commits (feat:, fix:, refactor:, docs:, test:)
- Never commit: bin/, obj/, .vs/, *.user, appsettings.*.json (except .Development)
- Always check for secrets before committing appsettings files
